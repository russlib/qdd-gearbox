*.exe
*.o
*.obj
*.dll
*.lib
*.exp
*.ilk
*.pdb

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
venv/
.venv/
.venv-*/
*.egg

# Workflow System
.context/       # Session state (personal, not for repo)
.superpowers/   # Brainstorm mockups and session data
logs/           # Daily engineering logs (commit selectively)
tools/*.exe     # Compiled utilities (regenerate locally)

# Generated output (regenerate with scripts, don't track in git)
*.stl
*.step
*.tar.gz
*.zip

# QDD test run outputs (keep tooling and handoff notes, not raw bench logs)
testing/data/synced-*/
testing/data/scratch/
testing/data/*.csv
testing/data/*.log
testing/data/*.png
testing/dyno/ble-capture/runs/*.csv
testing/dyno/ble-capture/runs/*.png

# Keep _versions/ snapshots tracked (override above)
!pygeartrain/step_output_aaron/_versions/
docs/_archive/hackathon-day-p1/
